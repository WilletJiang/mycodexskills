interface:
  display_name: "Brand Guidelines"
  short_description: "Apply brand styling with render checks."
  default_prompt: "Use $brand-guidelines to apply Anthropic colors and typography, then verify the rendered artifact."

policy:
  allow_implicit_invocation: true
