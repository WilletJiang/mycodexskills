interface:
  display_name: "Canvas Design"
  short_description: "Create static visuals with render checks."
  default_prompt: "Use $canvas-design to create an original PNG or PDF composition and inspect the final rendered output."

policy:
  allow_implicit_invocation: true
