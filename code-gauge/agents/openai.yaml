interface:
  display_name: "Code Gauge"
  short_description: "Plan risky code changes before editing."
  default_prompt: "Use $code-gauge to inspect constraints, simplify the approach, and define verification before changing code."

policy:
  allow_implicit_invocation: true
