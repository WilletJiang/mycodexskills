interface:
  display_name: "Web Application Testing"
  short_description: "Build Playwright visual harnesses."
  default_prompt: "Use $webapp-testing to verify rendered UI, interactions, console output, screenshots, and responsive behavior."

policy:
  allow_implicit_invocation: true
