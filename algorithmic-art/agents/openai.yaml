interface:
  display_name: "Algorithmic Art"
  short_description: "Create p5.js art with visual verification."
  default_prompt: "Use $algorithmic-art to create reproducible generative art and verify the rendered canvas, controls, and seed behavior."

policy:
  allow_implicit_invocation: true
