interface:
  display_name: "LaTeX Formula Polish"
  short_description: "Write formula-heavy LaTeX with a typesetting harness."
  default_prompt: "Use $latex-formula-polish to write or revise the mathematical LaTeX together with compile checks, warning scans, and affected-page visual inspection."

policy:
  allow_implicit_invocation: true
