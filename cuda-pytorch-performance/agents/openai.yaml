interface:
  display_name: "CUDA PyTorch Performance"
  short_description: "Build CUDA paths with a GPU harness."
  default_prompt: "Use $cuda-pytorch-performance to implement the CUDA or GPU path together with build, launch, correctness, and remote benchmark harnesses."

policy:
  allow_implicit_invocation: true
