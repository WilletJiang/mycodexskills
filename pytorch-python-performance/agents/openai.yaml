interface:
  display_name: "PyTorch Python Performance"
  short_description: "Build PyTorch code with a performance harness."
  default_prompt: "Use $pytorch-python-performance to implement the PyTorch path together with correctness checks, benchmark entry points, and a remote GPU run recipe."

policy:
  allow_implicit_invocation: true
