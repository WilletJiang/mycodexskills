interface:
  display_name: "PDF Workflows"
  short_description: "Read, render, edit, and validate PDF files."
  default_prompt: "Use $pdf to process the PDF with the right extraction, rendering, OCR, editing, or form workflow."

policy:
  allow_implicit_invocation: true
