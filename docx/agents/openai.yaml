interface:
  display_name: "DOCX Workflows"
  short_description: "Work with Word files using render checks."
  default_prompt: "Use $docx to create or edit the Word document while preserving formatting and verifying the rendered result."

policy:
  allow_implicit_invocation: true
